/**
 * Re-export challenge config from the shared package.
 * This file exists for backward compatibility with existing imports.
 */
export { RAMADAN_FORM } from "@halaqat/types";
export type { FormQuestion, QuestionType } from "@halaqat/types";