import { describe, it, expect } from "@jest/globals";

describe.skip("StudentPortalController (http)", () => {
  it("should return 401 without token", async () => {
    expect(true).toBe(true);
  });
});

