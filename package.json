{
  "name": "halaqat",
  "version": "1.0.0",
  "private": true,
  "description": "Halaqat - Mosque Management System",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.4.2",
    "rimraf": "^6.1.2",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.3",
  "engines": {
    "node": ">=18.0.0"
  }
}
