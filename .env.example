# ===========================================
# Halaqat Environment Configuration
# ===========================================

# --- 1. System Config ---
NODE_ENV=production
# (Next.js config handles it)
STANDALONE=true 

# --- 2. Database (Docker Creation) ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=halaqat
POSTGRES_PASSWORD=halaqat_secret_2024
POSTGRES_DB=halaqat_db

# --- 3. pgAdmin ---
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@halaqat.local
PGADMIN_DEFAULT_PASSWORD=admin123

# --- 4. Backend (NestJS) ---
DB_HOST=postgres
DB_PORT=5432
DB_USER=halaqat
DB_PASSWORD=halaqat_secret_2024
DB_NAME=halaqat_db
PORT=3001

# Authentication
JWT_SECRET=halaqat_jwt_secret_key_2024_very_secure
JWT_EXPIRATION=1d

# CORS
FRONTEND_URL=http://localhost:3000

# --- 5. Frontend (Next.js) ---
NEXT_PUBLIC_API_URL=http://localhost:3001/api